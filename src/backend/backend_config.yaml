llm:
  # Path to the GGUF model file
  # Env: CADAGENT_MODEL_PATH
  model_path: "/mnt/wslg/distro/home/bacchus/.cache/huggingface/hub/models--bartowski--Mistral-Nemo-Instruct-2407-GGUF/snapshots/a2dd64a0a76ea1bdb2bb6ab6fa5496b003c7c908/Mistral-Nemo-Instruct-2407-Q4_K_M.gguf"

  # Context window size (tokens)
  # Env: LLM_N_CTX
  n_ctx: 16384

  # Number of CPU threads for inference
  # Env: LLM_N_THREADS
  n_threads: 8

  # GPU layers to offload: 0=CPU-only, -1=all layers, N=specific number
  # Env: LLM_N_GPU_LAYERS
  n_gpu_layers: -1

  # Which GPU to use (for multi-GPU systems)
  # Env: LLM_MAIN_GPU
  main_gpu: 0